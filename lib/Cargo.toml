[package]
name = "random-icon-lib"
version = "0.1.1"
edition = "2021"
description = "Generates a random icon from a template."
keywords = ["random", "icon", "svg"]
categories = ["encoding"]
readme = "readme/README.md"
license = "MIT"

[features]
rand = ["dep:rand"]

[dependencies]
svg = "0.18.0"
rand = { version = "0.9.0", optional = true }
